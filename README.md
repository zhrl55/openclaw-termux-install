# OpenClaw Termux Installer

这是一个用于 **在 Android（Termux）上原生运行 OpenClaw 的一键安装脚本**。  
无需 root、无需 proot，适合旧手机作为 7×24 小时轻量服务器。

---

## 🚀 一键安装

在 Termux 中执行：

```bash
bash <(curl -L https://raw.githubusercontent.com/zhrl55/openclaw-termux-install/main/openclaw_install.sh)
